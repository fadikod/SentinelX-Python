# Local development — point at the Django dev server
VITE_API_URL=http://localhost:8000

# Vercel / production — leave VITE_API_URL unset (empty = same-origin)
# The vercel.json rewrites /api/* to the Python function automatically
